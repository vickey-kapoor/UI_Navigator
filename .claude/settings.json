{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd C:/Users/vicke/OneDrive/Documents/GitHub/UI_Navigator && python -m pytest tests/test_api.py tests/test_webpilot_api.py tests/test_sessions.py tests/test_clarifier.py -q --tb=short 2>&1 | tail -8"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import sys, json; d=json.load(sys.stdin); p=d.get('tool_input', {}).get('file_path', ''); name=p.replace('\\\\', '/').split('/')[-1]; sys.exit(1) if name == '.env' else None\""
          }
        ]
      }
    ]
  }
}
